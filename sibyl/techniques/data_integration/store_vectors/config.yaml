technique: store_vectors
description: "Store and manage vectors in vector databases"
version: "1.0.0"
default_subtechnique: storer
default_variant: default
timeout: 300
max_retries: 3
enabled: true
subtechniques:
  - storer
  - get_stats
