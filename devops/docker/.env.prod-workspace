# ============================================================================
# Sibyl Production Workspace Configuration
# ============================================================================
# Use this configuration for production deployment with cloud LLM providers
# (e.g., OpenAI) and production-grade infrastructure.
#
# Before using, set these environment variables:
#   export OPENAI_API_KEY="your-api-key"
#   export ANTHROPIC_API_KEY="your-api-key"  # if using Anthropic
#
# Usage:
#   docker-compose --env-file .env.prod-workspace --profile multi-workspace up -d
#
# ============================================================================

# Workspaces
SIBYL_WORKSPACE_LOCAL=config/workspaces/prod_web_research.yaml
SIBYL_WORKSPACE_PROD=config/workspaces/prod_web_research.yaml

# MCP Servers
MCP_HTTP_HOST=0.0.0.0
MCP_HTTP_PORT=8770
MCP_PROD_WORKSPACE_PORT=8772

# HTTP Servers
HTTP_PROD_WORKSPACE_PORT=8001

# Logging
MCP_LOG_LEVEL=INFO
MCP_JSON_LOGS=true

# Resources (Higher for production)
DUCKDB_MEMORY_LIMIT=2GB

# Build
BUILD_TARGET=runtime
VERSION=latest

# ============================================================================
# Production Notes:
# - Requires OPENAI_API_KEY environment variable
# - Recommended: Use separate deployments per workspace
# - Enable observability profile for monitoring:
#   --profile prod --profile observability
# - Enable TLS profile for HTTPS:
#   --profile prod --profile tls
# ============================================================================
