# MCP Server Dependencies

# Model Context Protocol SDK from Anthropic
mcp>=0.1.0

# HTTP Server (for VS Code side channel)
starlette>=0.27.0
uvicorn>=0.23.0

# Graph data structure (Phase 2: Resource Layer)
networkx>=3.2.1

# State management (Phase 1.2: DuckDB state storage)
duckdb>=0.9.0

# Async utilities
asyncio-mqtt>=0.16.0

# Logging and utilities
python-dotenv>=1.0.0
requests>=2.31.0
psutil>=5.9.0

# Phase 5: Vector search (semantic model search)
# Install separately: pip install fastembed
# Note: sqlite-vec requires manual installation (see README.md)
fastembed>=0.2.0

# Phase 4: Performance & Intelligence Improvements

# FAISS for ANN search (scalable vector search)
faiss-cpu>=1.7.4

# Git integration for pattern learning
GitPython>=3.1.40

# Machine learning for pattern clustering
scikit-learn>=1.3.0

# SQL linting for best practices
sqlfluff>=2.3.0

# Code complexity metrics
radon>=6.0.0

# Development and testing dependencies
pytest-benchmark>=4.0.0  # Performance testing
pytest-cov>=4.1.0        # Coverage reports
hypothesis>=6.92.0       # Property-based testing
